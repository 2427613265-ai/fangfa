# patent-drafting

方法和结构专利申请的 skill 分组。架构与启用顺序见仓库根目录 `README.md` 与 `draft-patent/SKILL.md`。

`understand-disclosure` 已补充理解顺序，并规定先用 Cursor Plan、再一轮一步输出。其余撰写 skill 仍只有职责与契约。

`reinforce-inventiveness` 不在默认全流程中；用户提到创造性不够或要求补强时再启用。
